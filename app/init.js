function init(){
  var gameStates = {planetHealth: 100, musicCount: 5};
  engine.setStates(gameStates);
  engine.setTitle("Hello World");
  //engine.playMusic(); Uncomment when there is music so the game does not crash
}
